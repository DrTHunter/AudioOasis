{
  "name": "audiooasis-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute audiooasis-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute audiooasis-db --local --file=./schema.sql"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
